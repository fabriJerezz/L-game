Class {
	#name : #Button,
	#superclass : #Object,
	#instVars : [
		'label',
		'action'
	],
	#category : #LGameClasses
}

{ #category : #abstract }
Button >> Click [
	"Método para manejar la acción que se realiza con el click en un botón"

]

{ #category : #initialization }
Button >> initializeWithLabel [
	"Inicializar botón con una etiqueta"

]
